@model ProjectUpdateViewModel
@{
    ViewData["Title"] = "Change failure rate";
}

<h2>Change failure rate updates</h2>
<div class="row">
    <div class="col-md-6">
        <span>Project:</span> <select asp-for="ProjectId" asp-items="Model.ProjectList" class="form-control">
            <option value="">Select Project</option>
        </select>
    </div>
</div>
@*<div class="row">
    <div class="col-md-6">
        <span>Complete %:</span> <select asp-for="CompletionPercent" asp-items="Model.CompleteList" class="form-control">
            <option value="">Select %</option>
        </select>
    </div>
</div>*@
